services:
  - type: web
    name: times-and-more
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --worker-class sync --bind 0.0.0.0:$PORT --timeout 180
    envVars:
      - key: FLASK_ENV
        value: production
